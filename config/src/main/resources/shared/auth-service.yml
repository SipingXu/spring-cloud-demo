server:
  port: 9100
  servlet:
    context-path: /uaa

spring:
  data:
    arangodb:
      hosts: localhost:8529
      user: ${ARANGO_USER_NAME}
      password: ${ARANGO_USER_PASSWORD}
      database: piggymetrics
